version: 1.0
runtime: python311
build:
  commands:
    pre-build:
      - python --version
      - python -m pip --version
    build:
      - python -m pip install --no-cache-dir -r requirements.txt
run:
  command: python app.py
  network:
    port: 8080
