version: 1.0
runtime: python311
build:
  commands:
    pre-build:
      - python3 --version
      - pip3 --version
    build:
      - pip3 install --no-cache-dir -r requirements.txt
run:
  command: python3 app.py
  network:
    port: 8080
